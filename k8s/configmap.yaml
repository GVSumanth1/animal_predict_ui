apiVersion: v1
kind: ConfigMap
metadata:
  name: animal-predict-config
  namespace: animal-predict
data:
  NODE_ENV: "production"
  NEXTAUTH_URL: "https://your-domain.com"  # Update with your domain
  PREDICTION_SERVICE_URL: "http://prediction-service:8080"  # Update with your service