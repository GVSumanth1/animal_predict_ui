apiVersion: v1
kind: Service
metadata:
  name: animal-predict-service
  namespace: animal-app-namespace
  labels:
    app: animal-predict
spec:
  type: LoadBalancer
  loadBalancerIP: 34.159.209.173
  selector:
    app: animal-predict
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  sessionAffinity: None